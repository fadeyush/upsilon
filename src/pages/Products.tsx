import React, { FC} from 'react';
import MyTabPanel from '../components/tabs/MyTabPanel';

const Products: FC = () => {
    return (
        <main>
            <MyTabPanel/>
        </main>
    );
};

export default Products;